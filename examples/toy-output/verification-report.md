# Verification Report

Verdict: `PASS`

For any integer `n`, the integers `n` and `n+1` are consecutive, so one is even. Therefore `n(n+1)` is even. Similarly, `n` and `n-1` are consecutive, so `n(n-1)` is even.

The nearby statement `n^2+1 is even` is not part of the locked target and is false at `n=0`.
